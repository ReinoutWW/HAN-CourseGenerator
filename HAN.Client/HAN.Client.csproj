<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

    <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly" Version="8.0.10"/>
        <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.DevServer" Version="8.0.10" PrivateAssets="all"/>
        <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.Authentication" Version="8.0.10"/>
        <PackageReference Include="Microsoft.EntityFrameworkCore.InMemory" Version="8.0.10" />
        <PackageReference Include="Radzen.Blazor" Version="5.6.7" />
    </ItemGroup>

    <ItemGroup>
      <ProjectReference Include="..\Data\HAN.Data.csproj" />
      <ProjectReference Include="..\HAN.Domain\HAN.Domain.csproj" />
      <ProjectReference Include="..\HAN.Repositories\HAN.Repositories.csproj" />
      <ProjectReference Include="..\HAN.Services\HAN.Services.csproj" />
      <ProjectReference Include="..\HAN.Tests\HAN.Tests.csproj" />
    </ItemGroup>

    <Target Name="RunTestsAfterBuild" AfterTargets="Build">
        <Exec Command="dotnet test" />
    </Target>

    <!-- Define PreBuild Target -->
    <Target Name="PreBuild" BeforeTargets="Build" Condition="'$(RunSonarScan)' == 'true'">
        <Exec Command="echo Running Pre-Build Command for SonarScan..." />
        <!-- Replace with your actual pre-build command -->
        <Exec Command='dotnet sonarscanner begin /k:"ReinoutWW_HAN-CourseGenerator_6e062695-950c-462b-892c-607a5f25b12d" /d:sonar.host.url="http://fraction1.local:30007"  /d:sonar.token="sqp_2a8ecbfd319def4d8e0abd3a9963f8446fb611da"
' />
    </Target>

    <!-- Define AfterBuild Target -->
    <Target Name="AfterBuild" AfterTargets="Build" Condition="'$(RunSonarScan)' == 'true'">
        <Exec Command="echo Running Post-Build Command for SonarScan..." />
        <!-- Replace with your actual post-build command -->
        <Exec Command='dotnet sonarscanner end /d:sonar.token="sqp_2a8ecbfd319def4d8e0abd3a9963f8446fb611da"' />
    </Target>


</Project>
